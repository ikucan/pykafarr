language: python
#sudo: required

services:
  - docker

before_install:
  - cd dckr/build
  - docker build iztokkucan/pykafarr_build
  
install:
  - pip install -r .requirements.txt
  
script:
  - pwd

matrix:
    include:
        - dist: xenail
          python: 3.7
